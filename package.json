{
  "name": "acxmc-portfolio",
  "version": "1.0.0",
  "main": "",
  "scripts": {
      "start": "npm start --prefix server",
      "install-client": "cd client && npm install && npm run serve",
      "heroku-postbuild": "npm run install-client"
  }
}